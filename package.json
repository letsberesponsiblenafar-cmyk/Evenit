{
  "scripts": {
    "preview": "node scripts/preview.mjs",
    "check": "node scripts/check.mjs",
    "test": "npm run check && node --test scripts/test-navigation.mjs scripts/test-live-updates.mjs scripts/test-camera.mjs scripts/test-aftermath.mjs",
    "mobile:copy": "node scripts/build-mobile.mjs",
    "mobile:sync": "npm run mobile:copy && npx cap sync android",
    "android:apk": "npm run mobile:sync && cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "supabase": "^2.115.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/network": "^8.0.1"
  }
}
